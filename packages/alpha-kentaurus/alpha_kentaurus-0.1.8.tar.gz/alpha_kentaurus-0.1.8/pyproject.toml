[tool.poetry]
name = "alpha_kentaurus"
version = "0.1.8"
description = "Ah ah ah, you didn't say the magic word"
authors = ["nope"]
homepage = "https://www.youtube.com/watch?v=RfiQYRn7fBg"
readme = "README.md"
packages = [{include = "alpha_kentaurus", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.7"

[tool.poetry.scripts]
alpha = 'alpha.main:main'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
