from setuptools import find_packages, setup

setup(
    name='netos-inventory',
    version='0.2.1',
    description='An reconciliation plugin for NetBox',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
