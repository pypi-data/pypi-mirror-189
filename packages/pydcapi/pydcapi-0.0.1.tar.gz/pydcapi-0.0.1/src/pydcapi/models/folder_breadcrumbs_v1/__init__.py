# generated by datamodel-codegen:
#   filename:  folder_breadcrumbs_v1.json

from __future__ import annotations

from typing import List

from pydantic import AnyUrl, BaseModel, Extra, Field


class Breadcrumb(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    name: str = Field(..., description='Name of the folder ("/" for root).')
    uri: AnyUrl = Field(..., description="URI representing the folder")


class Model(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    breadcrumbs: List[Breadcrumb] = Field(
        ...,
        description="Folders in hierarchy order from root to this folder, inclusive.",
    )
