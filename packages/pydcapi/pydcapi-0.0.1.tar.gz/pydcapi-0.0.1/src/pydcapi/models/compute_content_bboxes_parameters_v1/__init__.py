# generated by datamodel-codegen:
#   filename:  compute_content_bboxes_parameters_v1.json

from __future__ import annotations

from typing import Optional

from pydantic import AnyUrl, BaseModel, Extra, Field


class Model(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    asset_uri: AnyUrl = Field(..., description="URI identifying the Asset")
    pages: str = Field(
        ...,
        description="A page range in the format X (a single page) or X-Y (a range of pages). Page numbers are 1-based. A hyphen after a page number will not be considered unless followed by another page number. E.g : 2-3 is a valid page range, 2- is not. Page range difference should not be greater than 10.",
    )
    partial: Optional[bool] = Field(
        None,
        description="Optional flag to specify if partial information of Bboxes is required. If flag is false or not-present, we use the option based on the user type- false for paid users and true for free users regardless of the param",
    )
