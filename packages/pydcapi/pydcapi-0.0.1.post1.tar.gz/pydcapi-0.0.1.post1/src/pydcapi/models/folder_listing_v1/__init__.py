# generated by datamodel-codegen:
#   filename:  folder_listing_v1.json

from __future__ import annotations

from datetime import datetime
from typing import List, Optional, Union

from pydantic import AnyUrl, BaseModel, Extra, Field


class FieldPages(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    next_page_uri: Optional[AnyUrl] = Field(
        None,
        description="URI used to fetch the next page of results in this folder listing.  Only present if there is another page of results.",
    )


class Member(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    asset_uri: AnyUrl = Field(
        ..., description="URI representing a member that's an asset"
    )
    name: str = Field(..., description="Name of the asset.")
    asset_id: Optional[str] = Field(
        None,
        description='The asset id (in URN form).  Use the "asset_uri" uri template in the "templates" section of the discovery info to transform to an asset uri.',
    )
    parent_uri: Optional[AnyUrl] = Field(
        None,
        description="URI of the parent folder of this asset.  If there is no parent (e.g. for a transient asset), this value is not present.",
    )
    parent_id: Optional[str] = Field(
        None,
        description="Folder id (in URN form) of the parent folder of this asset.  If there is no parent (e.g. for a transient asset), this value is not present.",
    )
    size: Optional[float] = Field(None, description="File size in bytes")
    type: Optional[str] = Field(None, description="The file's content type (mime-type)")
    md5_digest: Optional[str] = Field(
        None,
        description="The hex digest of 128 bit MD5 digest of the asset - a 32 character string containing only hexadecimal digits.  May not be available immediately after uploading or updating a file.",
    )
    page_count: Optional[float] = Field(
        None,
        description="The number of pages in the asset.  Only present for assets in which the page count has been determined.  Page count discovery happens while generating renditions for supported asset types (e.g. pdf).",
    )
    created: Optional[datetime] = Field(
        None, description="The creation date of the asset."
    )
    modified: Optional[datetime] = Field(
        None, description="The modification date of the asset."
    )
    last_access: Optional[datetime] = Field(
        None, description="The time the asset was last accessed."
    )
    last_pagenum: Optional[float] = Field(
        None,
        description="The number of the page last viewed.  This number is 0 based - the first page is 0, second page is 1, etc.",
    )
    source: Optional[str] = Field(
        None,
        description='Where the asset resides.  Currently either "native" or "creative_cloud"',
    )
    created_by_client: Optional[str] = Field(
        None, description="An indication of which API client first uploaded the asset."
    )
    favorite: Optional[bool] = Field(
        None, description="True if the user has marked this asset a favorite."
    )
    starred: Optional[bool] = Field(
        None, description="True if the user has starred this asset."
    )
    tags: Optional[List[str]] = Field(None, description="An array of tags.")
    custom_tags: Optional[List[str]] = Field(
        None, description="An array of custom tags for client application use."
    )
    sign: Optional[str] = Field(
        None,
        description="sign metadata for platform users (Signatures experience in Acrobat),Proposed sign-schema :https://git.corp.adobe.com/caf/xcm/pull/136/files.",
    )


class Member1(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    folder_uri: AnyUrl = Field(
        ..., description="URI representing a member that's a folder"
    )
    name: str = Field(..., description="Name of the folder.")
    folder_id: Optional[str] = Field(
        None,
        description='The folder id (in URN form).  Use the "folder_uri" uri template in the "templates" section of the discovery info to transform to a folder uri.',
    )
    parent_uri: Optional[AnyUrl] = Field(
        None,
        description="URI of the parent folder of this folder.  If there is no parent (e.g. for a transient folder), this value is not present.",
    )
    parent_id: Optional[str] = Field(
        None,
        description="Folder id (in URN form) of the parent folder of this folder. If there is no parent (e.g. for a transient folder), this value is not present.",
    )
    created: Optional[datetime] = Field(
        None, description="The creation date of the folder."
    )
    modified: Optional[datetime] = Field(
        None, description="The modification date of the folder."
    )
    source: Optional[str] = Field(
        None,
        description='Where the folder resides.  Currently either "native" or "creative_cloud"',
    )
    favorite: Optional[bool] = Field(
        None, description="True if the user has marked this folder a favorite."
    )
    starred: Optional[bool] = Field(
        None, description="True if the user has starred this folder."
    )
    tags: Optional[List[str]] = Field(None, description="An array of tags.")
    custom_tags: Optional[List[str]] = Field(
        None, description="An array of custom tags for client application use."
    )


class Model(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    name: str = Field(..., description="Name of the folder.")
    uri: AnyUrl = Field(..., description="URI representing the folder")
    parent_uri: Optional[AnyUrl] = Field(
        None,
        description="URI of the parent folder of this folder.  If there is no parent (e.g. for the root), this value is not present.",
    )
    total_members: float = Field(
        ..., description="The total number of items in this folder."
    )
    field_pages: FieldPages = Field(
        ..., alias="_pages", description="Information for paging through results."
    )
    members: List[Union[Member, Member1]] = Field(
        ..., description="An array of objects representing the members of this folder."
    )
