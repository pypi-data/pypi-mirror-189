# generated by datamodel-codegen:
#   filename:  recent_searches_v1.json

from __future__ import annotations

from typing import List, Optional

from pydantic import BaseModel, Extra, Field, conint, constr


class ScopeItem(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    __root__: constr(min_length=1, max_length=256)


class UssPacket(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    q: constr(min_length=1, max_length=1024)
    scope: List[ScopeItem] = Field(..., max_items=10, min_items=0)


class RecentSearch(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    uss_packet: UssPacket = Field(..., description="A uss packet in JSON format.")
    created: Optional[conint(ge=0)] = Field(
        None, description="The UNIX UTC timestamp of when the recent search was created"
    )


class Model(BaseModel):
    class Config:
        extra = Extra.allow
        allow_mutation = False

    recent_searches: List[RecentSearch] = Field(
        ...,
        description="An array of USS packets along with their timestamps.",
        max_items=25,
        min_items=0,
    )
