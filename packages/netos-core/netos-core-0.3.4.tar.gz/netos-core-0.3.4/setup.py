from setuptools import find_packages, setup

setup(
    name='netos-core',
    version='0.3.4',
    description='An reconciliation plugin for NetBox',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
