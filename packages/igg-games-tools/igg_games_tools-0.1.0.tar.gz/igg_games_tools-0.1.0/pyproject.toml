[tool.poetry]
name = "igg-games-tools"
version = "0.1.0"
description = ""
authors = ["jawide <jawide@qq.com>"]
readme = "README.md"
packages = [{include = "igg_games_tools"}]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.2"

[tool.poetry.scripts]
igt = "igg_games_tools.__main__:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
