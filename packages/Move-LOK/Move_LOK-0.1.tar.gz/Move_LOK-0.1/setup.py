from setuptools import setup, find_packages

setup(
    name='Move_LOK',
    version='0.1',
    description='A package for moving LOK',
    author='Chapelle',
    packages=find_packages(),
    install_requires=[],
)
