[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Bignumber_to_PersianString_sama"
version = "0.0.1"
authors = [
  { name="sama", email="samaj.aut@gmail.com" },
]
description = "A small package to cnvert big numbers to Persian"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

