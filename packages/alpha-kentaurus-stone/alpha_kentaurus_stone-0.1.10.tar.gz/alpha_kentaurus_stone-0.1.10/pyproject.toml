[tool.poetry]
name = "alpha_kentaurus_stone"
version = "0.1.10"
description = "Ah ah ah, you didn't say the magic word"
authors = ["nope"]
homepage = "https://www.youtube.com/watch?v=RfiQYRn7fBg"
readme = "README.md"
packages = [{include = "alpha_kentaurus", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.9.0"

[tool.poetry.scripts]
alpha = 'alpha_kentaurus_stone.main:main'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
