[tool.poetry]
name = "tag-blacklight"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
license = "UNLICENSED"
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.scripts]
blacklight = "src.example:run"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
