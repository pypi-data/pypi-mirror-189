from setuptools import find_packages, setup

setup(
    name='netos_reporting',
    version='0.3.0',
    description='A plugin with chart based reports',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
