from setuptools import find_packages, setup

setup(
    name='netos-fabric',
    version='0.2.0',
    description='An fabric plugin for NetBox',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
