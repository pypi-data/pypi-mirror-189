from setuptools import setup, find_packages
import codecs
import os

setup(
    name="discordreq1",
    author="zTeemo",
    long_description_content_type="text/markdown",
    packages=find_packages(),
    install_requires=[],
    keywords=['python', 'discord', 'fast api', 'best discord request'],
    classifiers=[
        "Operating System :: Microsoft :: Windows"
    ]
)
